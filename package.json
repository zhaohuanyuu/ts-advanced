{
  "name": "ts-advanced",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "build": "tsc && vite build",
    "debug": "tsc && vite build --watch "
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "vite": "^2.9.5"
  }
}
